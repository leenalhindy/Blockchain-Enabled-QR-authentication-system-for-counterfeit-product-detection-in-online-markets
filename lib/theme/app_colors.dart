import 'package:flutter/material.dart';

class AppColors {
  static const Color white = Color(0xFFFFFFFF);

  // خلفيات ناعمة
  static const Color light = Color(0xFFF7F5FF);
  static const Color light2 = Color(0xFFEDE7FB);

  // درجات بطاقة / container
  static const Color mid = Color(0xFFD4C8F2);

  // Gradient + عناصر كبيرة
  static const Color deep = Color(0xFFA795D9);

  // نصوص + أيقونات + أزرار داكنة
  static const Color dark = Color(0xFF6E57A5);
}
